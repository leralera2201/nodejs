{{#each users}}
    <div class="card">
        <div class="card-body">
            <blockquote class="blockquote mb-0">
                <p>{{this.name}}</p>
                <footer class="blockquote-footer">{{this.email}}</footer>
            </blockquote>
        </div>
    </div>
{{/each}}

